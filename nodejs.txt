常用node包:
使用supervisor提高nodejs调试效率
npm -g install supervisor
supervisor必须安装到全局,如果你不安装到全局，错误命令会提示你安装到全局,如果不想安装到默认的全局,也可以自己修改全局路径到当前路径
npm config set prefix "路径"


$ sudo npm install forever -g   #安装
$ forever start app.js          #启动
$ forever stop app.js           #关闭
$ forever start -l forever.log -o out.log -e err.log app.js   #输出日志和错误

forever stop /root/zp/bin/www
forever start /root/zp/bin/www
forever start -a -l forever.log -o out.log -e err.log /root/zp/bin/www
forever stop -a -l forever.log -o out.log   -e err.log /root/zp/bin/www



重启:
方式一:
npm restart
方式二:
ps aux | grep node

kill -9 PRCOCESS_ID


删除文件夹实例：
rm -rf /var/log/httpd/access
删除文件使用实例：
rm -f /var/log/httpd/access.log


ls -a:显示所有文件
vim+文档名:打开文件
